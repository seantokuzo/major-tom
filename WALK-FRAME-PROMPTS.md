# Walk Animation Frame Prompts

Upload each character's CURRENT sprite sheet as reference, then paste the prompt.
Each generates a single "mid-stride" walk sheet — same character with legs in walking pose.
We alternate between idle + walk textures in-engine for a 2-frame walk cycle.

Mark `[x]` as you complete each one. Drop outputs in `assets/walk_frames/`

---

## Humans

### [ ] Dev — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same character — same outfit, same colors, same proportions — but with legs in a mid-stride walking pose (one leg forward, one leg back). The character should look like they are actively walking/stepping.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The character must be IDENTICAL to the reference — same clothes, same colors, same face — only the legs change to a walking pose.

---

### [ ] Office Worker — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same character — same outfit, same colors, same proportions — but with legs in a mid-stride walking pose (one leg forward, one leg back). The character should look like they are actively walking/stepping.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The character must be IDENTICAL to the reference — same clothes, same colors, same face — only the legs change to a walking pose.

---

### [ ] PM — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same character — same outfit, same colors, same proportions — but with legs in a mid-stride walking pose (one leg forward, one leg back). The character should look like they are actively walking/stepping.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The character must be IDENTICAL to the reference — same clothes, same colors, same face — only the legs change to a walking pose.

---

### [ ] Clown — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same character — same outfit, same colors, same proportions — but with legs in a mid-stride walking pose (one leg forward, one leg back). The character should look like they are actively walking/stepping.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The character must be IDENTICAL to the reference — same clothes, same colors, same face — only the legs change to a walking pose.

---

### [ ] Frankenstein — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same character — same outfit, same colors, same proportions — but with legs in a mid-stride walking pose (one leg forward, one leg back). The character should look like they are actively walking/stepping.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The character must be IDENTICAL to the reference — same clothes, same colors, same face — only the legs change to a walking pose.

---

### [ ] Dwight — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same character — same outfit, same colors, same proportions — but with legs in a mid-stride walking pose (one leg forward, one leg back). The character should look like they are actively walking/stepping.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The character must be IDENTICAL to the reference — same clothes, same colors, same face — only the legs change to a walking pose.

---

### [ ] Michael — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same character — same outfit, same colors, same proportions — but with legs in a mid-stride walking pose (one leg forward, one leg back). The character should look like they are actively walking/stepping.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The character must be IDENTICAL to the reference — same clothes, same colors, same face — only the legs change to a walking pose.

---

## Dogs

> Dogs are trickier — walking means legs spread further apart, body stays low and horizontal.
> Side views should show the classic "trot" pose with legs extended.

### [ ] Elvis — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same dog — same space suit, same colors, same proportions — but with legs in a mid-stride TROTTING pose. Front/back views: legs slightly spread. Side views: classic dog trot with front and back legs extended in opposite directions.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Dogs are WIDER than tall in side views. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The dog must be IDENTICAL to the reference — same outfit, same colors, same face — only the legs change to a trotting pose.

---

### [ ] Senor — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same dog — same green hoodie, same colors, same proportions — but with legs in a mid-stride TROTTING pose. Front/back views: legs slightly spread. Side views: classic dachshund trot with short legs extended in opposite directions, long body visible.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Dachshunds are LONG — side views much wider than tall. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The dog must be IDENTICAL to the reference — same outfit, same colors, same face — only the legs change to a trotting pose.

---

### [ ] Steve — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same dog — same space suit, same colors, same proportions — but with legs in a mid-stride TROTTING pose. Front/back views: legs slightly spread. Side views: classic cattle dog trot with legs extended in opposite directions.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Dogs are WIDER than tall in side views. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The dog must be IDENTICAL to the reference — same outfit, same colors, same face — only the legs change to a trotting pose.

---

### [ ] Esteban — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same dog — same bow tie, same colors, same proportions — but with legs in a mid-stride TROTTING pose. Front/back views: legs slightly spread. Side views: classic cattle dog trot with legs extended in opposite directions.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Dogs are WIDER than tall in side views. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The dog must be IDENTICAL to the reference — same outfit, same colors, same face — only the legs change to a trotting pose.

---

### [ ] Hoku — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same dog — same space suit, same colors, same proportions — but with legs in a mid-stride TROTTING pose. Front/back views: legs slightly spread. Side views: classic schnauzer trot with legs extended in opposite directions, beard bouncing.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The dog must be IDENTICAL to the reference — same outfit, same colors, same face — only the legs change to a trotting pose.

---

### [ ] Kai — Walk Frame

I'm uploading this character's idle sprite sheet. Generate a MATCHING walk cycle version of this EXACT same dog — same space suit, same tongue-out personality, same colors, same proportions — but with legs in a mid-stride TROTTING pose. Front/back views: legs slightly spread. Side views: classic schnauzer trot with legs extended in opposite directions.

32×32 pixel art. 4 views in a 2×2 grid on a single image: walk-front (top-left), walk-back (top-right), walk-left (bottom-left), walk-right (bottom-right). Each view clearly separated with empty space between them. Style: Stardew Valley / Shovel Knight — flat solid color blocks, ZERO gradients.

CRITICAL RULES — violating ANY of these means the image must be regenerated:
1. EVERY edge must be a hard 90-degree pixel step. ZERO anti-aliasing. ZERO edge smoothing. If you zoom in, each pixel boundary should be a sharp staircase, never a gradient.
2. Background MUST be perfectly transparent (PNG alpha = 0). No white, no gray, no gradient, no subtle tint.
3. NO ground shadow. NO drop shadow. NO ambient occlusion under the character. The character floats on pure transparency.
4. ALL shading must be done with FLAT solid color blocks — like Stardew Valley or Shovel Knight. Never use gradients or soft brushes.
5. NO glow effects, NO bloom, NO light halos around any part of the sprite.
6. The 4 views must be on a single canvas, clearly separated with empty space between them.
7. The dog must be IDENTICAL to the reference — same outfit, same colors, same face — only the legs change to a trotting pose.
